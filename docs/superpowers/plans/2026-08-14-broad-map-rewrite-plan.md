# Broad Map Runtime Rewrite Implementation Plan

1. Capture the clean-base runtime/event/performance baseline using the shared experiment matrix.
2. Add failing acceptance tests plus pure/integration contracts for every documented runtime phase/event, request-scoped stale commit/failure, pointer/touch/pen/keyboard compatibility deduplication, committed-overlay identity, prepared graph reuse/content invalidation/cancellation, and Option A facility/boarding-card clearance.
3. Build the pure map runtime reducer/controller and route engine with a latest-request, cancellable commit protocol.
4. Replace distributed page/navigation state derivation with the runtime controller while preserving persistence, announcements, external routing, manual start, and clear semantics.
5. Replace marker/background event overlap with the single interaction gateway and make marker rendering subscribe to stable presentation slices.
6. Keep one stable route overlay mounted from committed route state and update Leaflet geometry without transient unmounts.
7. Split status HUD, approved bottom action dock, and measured mini-card clearance into focused presentation components.
8. Add bounded performance instrumentation and graph/marker interaction benchmarks.
9. Run the exact commands and artifact protocol in `../specs/2026-08-14-map-experiment-comparison-protocol.md`, obtain independent spec/code/architecture review, and open an unmerged PR to public `main`.
